{
    "name": "project/commission-task-skeleton",
    "description": "Skeleton for Project recruitment task",
    "keywords": [],
    "type": "project",
    "autoload": {
        "psr-4": {
            "src\\": "src/",
            "tests\\": "tests/"
        }
    },
    "require": {
        "php": ">=8.2",
        "ext-bcmath": "*"
    },
    "require-dev": {
        "phpunit/phpunit": "^10.3"
    },
    "config": {
        "sort-packages": true,
        "bin-dir": "bin"
    },
    "scripts": {
        "phpunit": "phpunit",
        "test": ["@phpunit"]
    }
}
